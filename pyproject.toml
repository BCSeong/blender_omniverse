# Workspace root — 두 패키지를 묶는 모노레포 설정
[tool.uv.workspace]
members = ["packages/cam_sim", "packages/blender_authoring"]

[tool.pytest.ini_options]
testpaths = ["packages/cam_sim/tests", "packages/blender_authoring/tests"]
